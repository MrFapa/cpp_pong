#pragma once

// Window
const int WINDOW_WIDTH = 1000;
const int WINDOW_HEIGHT = 800;

// Puck
const float PUCK_SPEED = 200;
const float PUCK_SPEED_INCREMENT = 50;
const float PUCK_SIZE = 20; // Quadratic
const int PUCK_MAX_BOUNCE_ANGLE = 60; // DO NOT exceed 89deg, although everything above 70 is bad

// Paddles
const float PADDLE_WIDTH = 20;
const float PADDLE_HEIGHT = 200;

const float PADDLE_SPEED = 400;
